language: python

python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python3 -m unittest tests/test_thanosdb.py -v
  - python3 -m doctest -v thanosdb/thanosdb.py